<img src="https://cdn.prod.website-files.com/677c400686e724409a5a7409/6790ad949cf622dc8dcd9fe4_nextwork-logo-leather.svg" alt="NextWork" width="300" />

# Build a Three-Tier Web App

**Project Link:** [View Project](http://learn.nextwork.org/projects/aws-compute-threetier)

**Author:** naru uzu  
**Email:** rutu13patil@gmail.com

---

## Build a Three-Tier Web App

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_2b3c4d5e)

---

## Introducing Today's Project!

🪣 Create a storage bucket for your website's files with S3.

🌎 Distribute your content globally with CloudFront.

⚙️ Build the brains of your application using serverless functions with Lambda.

🚪 Create an API to handle user requests with API Gateway.

💾 Store and retrieve user data with DynamoDB.

🔗 Connect all these services together seamlessly for your three-tier architecture.



### Tools and concepts

🌎 The CloudFront distribution (do this first!)

🪣 The S3 bucket.

⚙️ The Lambda function.

🚪 The API.

💾 The DynamoDB table.

### Project reflection

This whole project took me three days as i did it step by step. The most challenging part was there were some errors but dicovering developer tools and how to solve these errors was a new experience. It was more rewarding to see the site displaying the user information as you know what all process it takes to get the data.

I chose to do this project today because it was a 3 tier project and was really worth it.

---

## Presentation tier

Create an S3 bucket to store your website's files.

Upload a simple index.html file to your bucket.

Set up CloudFront to deliver your website's content globally.

Use a global URL provided by your content delivery network rather than accessing your storage directly. Here is how that process works:

Upload the files: First, you upload your website's static files (such as index.html, style.css, and script.js) into an Amazon S3 bucket.

Distribute the content: Instead of making the S3 bucket public, you set up an Amazon CloudFront distribution to securely retrieve and deliver those files globally.

Get your URL: Once your Amazon CloudFront distribution is created, AWS generates a unique Distribution Domain Name (which is d38ir79xnxr2wc.cloudfront.net).

Access the site: You access your live website by copying that Distribution Domain Name and pasting it into your web browser's address bar.

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_3a4b5c6d)

---

## Logic tier

Create a Lambda function to fetch data from a DynamoDB table.

Write the code for your Lambda function.

Create an API Gateway REST API.

Create a resource and method to handle GET requests.

Deploy the API to make it accessible.



This code sets up a Lambda function that retrieves data from a DynamoDB table.

It looks for specific user data based on a userId and returns that data. If there's an error e.g. the userId doesn't exist in the database, it returns an error message.

Lambda functions can interact with other AWS services using the AWS SDK. The SDK gives you a set of libraries and tools for accessing various AWS services, such as DynamoDB, S3, and API Gateway.

In our case, we're using the DynamoDB functions within SDK to fetch data from our database.

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_6a7b8c9d)

---

## Data tier

Create a DynamoDB table.

Add user data into your table.

DynamoDB is one of AWS's database services. It stands out as a fast and flexible way to store data, which makes it a great choice for apps that need quick access to large volumes of data e.g. games.

A partition key is the heart of how DynamoDB organizes data. Think of it as a label that you can use to group similar items. Under the hood, the partition key is how DynamoDB spreads out your data across different servers for quick access and efficient querying.

Every item in your table must have a unique partition key.

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_u1v2w3x4)

---

## Logic and Data tier

Update your script.js file with JavaScript code to make an API request.

Verify that the data is displayed on your website.

Retrieved the Endpoint: You located and copied the unique Invoke URL generated by Amazon API Gateway after deploying your prod stage.

Constructed the Full URL: You appended the specific resource path (/users) and your query string parameter (?userId=1) to the end of the base URL.

Triggered a GET Request: You pasted this complete URL into your web browser's address bar and pressed Enter to send a live web request.

Verified the Output: You confirmed that the web page returned a successful JSON response directly from your AWS Lambda function (even if it was empty, a null, or a mock message, it proved the request successfully traveled through the API gateway and ran your code!).

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_a112c3d5)

---

## Console Errors

The error in my distributed site occurred because the script.js file still had a placeholder value '[YOUR-PROD-API-URL]' instead of my real API Gateway Invoke URL. Because of this, the browser tried to load data from an invalid web address and failed.

I uploaded an updated script.js file to replace the '[YOUR-PROD-API-URL]' placeholder with my actual API Gateway Invoke URL. Overwriting the old file in S3 ensures my frontend can successfully make network calls to fetch data from my database.

 The CORS (Cross-Origin Resource Sharing) error you're encountering happens because your API Gateway is not configured to allow requests from your CloudFront URL.

API Gateway is only allowing requests directly from its Invoke URL!

To resolve this, you'll need to enable CORS on your API Gateway so that it can accept requests from the domain where your frontend is hosted.



![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_a1b2c3d5)

---

## Resolving CORS Errors

In this case, because CORS isn’t configured in API Gateway, the browser freaks out and blocks the request from CloudFront, giving you those annoying CORS errors.

So, we need to enable CORS on your API Gateway. This is like handing the browser a VIP list that says, "Yep, the CloudFront URL is trusted - let it through."



You're using Lambda Proxy Integration for your GET method.

When proxy integration is enabled, API Gateway simply forwards the request to your Lambda function and expects the Lambda to return the full HTTP response, including the CORS headers.

This means the CORS headers must be added directly in your Lambda function response, not within API Gateway.


![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_1qthryj2)

---

## Fixed Solution

I verified the fixed solution by creating a CloudFront invalidation to force load the updated script.js, then accessing the site using my CloudFront Domain Name. I searched for a user ID and confirmed that data was successfully pulled from DynamoDB and displayed on the screen without any CORS or network errors in my browser's Console.

![Image](http://learn.nextwork.org/content_rose_beautiful_white_sapote/uploads/aws-compute-threetier_2b3c4d5e)

---

---
